# OCR Service - Python Dependencies
# Pi 5 / Python 3.13 compatible - uses system Tesseract (apt install tesseract-ocr)

# Web Framework (flexible for Python 3.11–3.13)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Image Processing (Pillow 11+ has Python 3.13 pre-built wheels on piwheels)
Pillow>=11.0.0

# OCR Engine
pytesseract>=0.3.10

# Utilities
numpy>=1.24.0
